// Update this once the real backend WebSocket endpoint exists
// (see "[Backend] Game loop + WebSocket server" issue). Until then it
// points at the local mock server in dev-tools/mock_ws_server.py.
export const WS_URL = 'ws://localhost:8765/ws';
